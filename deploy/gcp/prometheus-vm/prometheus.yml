global:
  scrape_interval: 15s

scrape_configs:
  - job_name: mitsubishi-rag-cloud-run
    scheme: https
    metrics_path: /metrics
    static_configs:
      - targets:
          - YOUR_CLOUD_RUN_HOSTNAME

# Example target value (hostname only, no https):
#   mitsubishi-rag-xxxxxxxxx-uc.a.run.app
